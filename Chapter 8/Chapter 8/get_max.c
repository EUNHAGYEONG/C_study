/*
#include <stdio.h>

int get_integer() {
	int value;

	printf("정수를 입력해주세요 : ");
	scanf_s("%d", &value);

	return value;
}

int get_max(int x, int y) {
	if (x > y) return x;
	else return y;
}

int main(void) {
	int num1, num2, max;

	num1 = get_integer();
	num2 = get_integer();

	max = get_max(num1, num2);

	printf("가장 큰 정수는 %d 입니다. \n", max);

	return 0;
}
*/