/*
#include <stdio.h>
#define SIZE 26

int main(void) {
	char codes[SIZE];
	char str = 'a';
	int i;

	for (i = 0; i < SIZE; i++) {
		codes[i] = str;
		printf("codes[%d] = %c \n", i, codes[i]);
		str++;
	}

	return 0;
}
*/