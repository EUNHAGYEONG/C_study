/*
#include <stdio.h>
#define SIZE 5

int main(void) {
	int scores[SIZE];
	int i = 0;
	int sum = 0;
	double avg = 0.0;

	for (i = 0; i < SIZE; i++) {
		printf("%d번째 학생의 점수를 입력하시오 : ", i + 1);
		scanf_s("%d", &scores[i]);

		sum += scores[i];
	}

	avg = sum / 5.0;
	
	printf("학생들의 평균 성적은 %f 입니다. \n", avg);
	return 0;
}
*/