/*
#include <stdio.h>
#define STUDENTS 5

int main(void) {
	int i, score[STUDENTS];
	int sum = 0;
	float avg = 0.0;

	for (i = 0; i < STUDENTS; i++) {
		printf("학생의 점수를 입력해주세요 : ");
		scanf_s("%d", &score[i]);

		sum += score[i];
	}

	avg = sum / 5.0;

	printf("전체 학생의 평균은 %f입니다. \n", avg);

	system("PAUSE");
	return 0;
}
*/
