/*
#include <stdio.h>
#define SIZE 5

int main(void) {
	int list[SIZE] = { 16, 7, 9, 1, 3 };
	int i, k, tmp, s;
	for (k = 0; k < SIZE; k++)
	{
		for (i = 0; i < SIZE - 1; i++) {
			if (list[i] > list[i + 1]) {
				tmp = list[i];
				list[i] =  list[i + 1];
				list[i + 1] = tmp;
			}
		}
	}
	for (s = 0; s < SIZE; s++)
		printf("%d ", list[s]);
	system("PAUSE");
	return 0;
}
*/
