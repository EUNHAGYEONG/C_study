/*
#include <stdio.h>
# define ROWS 3
# define COLS 3

int main(void) {
	int i, j;
	int A[ROWS][COLS] = { {1, 0, 0}, {0, 1, 0}, {0, 0, 1} };
	int B[ROWS][COLS] = { {1, 0, 0}, {0, 1, 0}, {0, 0, 1} };
	int sum[ROWS][COLS] = { 0 };

	for (i = 0; i < ROWS; i++) {
		for (j = 0; j < COLS; j++) {
			sum[i][j] = A[i][j] + B[i][j];
			printf("%d ", sum[i][j]);
		}
		printf("\n");
	}
	system("PAUSE");
	return 0;
}
*/
